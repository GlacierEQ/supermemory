# Requirements intentionally empty because the NAS module uses only the Python
# standard library. The file is kept so that the shared pretest script can
# continue installing from it without errors.
